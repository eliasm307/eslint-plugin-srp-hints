module.exports = {
	configs: require('./lib/configs'),
	rules: require('./lib/rules'),
	// utils: require('./lib/utils'), // ? remove
	// defaultOptions: require('./lib/defaultOptions'), // ? remove
};
